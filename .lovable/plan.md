
## Goal
Add a "Step 1 — Download Template" section at the top of the Employee Upload screen with a button that generates and downloads `bonusbridge_employee_template.xlsx` using SheetJS.

## Investigation findings
- No `employee-upload.tsx` route exists yet under `src/routes/_authenticated/`.
- Existing HR-Rep pages (`org-departments.tsx`, `register-entity.tsx`) use the `_authenticated` layout, page-level role gate (`hr_rep` / `ceo`), and shared UI primitives (`Button`, `Card`).
- No `xlsx` package installed yet.

## Files touched

### 1. New route: `src/routes/_authenticated/employee-upload.tsx`
- Standard `createFileRoute("/_authenticated/employee-upload")` pattern.
- Page-level role gate matching other HR pages: must include `hr_rep` or `ceo`; otherwise show "Access denied".
- Top section card titled **"Step 1 — Download Template"** with:
  - Short helper copy: "Download the template, fill it with your employee data, then upload it in Step 2."
  - Primary button **"Download Employee Template"** wired to `downloadEmployeeTemplate()`.
- Placeholder for "Step 2 — Upload File" (heading only, no logic) so the screen reads as a multi-step flow.

### 2. Template helper (inline in the route file)
Generates the workbook in-browser via SheetJS:
- Sheet name: `Employees`
- Row 1 headers (exact order):
  `employee_reference, first_name, last_name, email, org_department, functional_department, annual_salary, employment_start_date, role`
- Row 2 example values:
  `EMP001, Jane, Smith, jane.smith@company.com, HQ — Commercial, Sales / Marketing, 75000, 2024-01-15, employee`
- Apply grey italic styling to every cell in row 2 (`{ font: { italic: true, color: { rgb: "808080" } } }`).
- Set sensible `!cols` widths so headers are readable.
- Trigger download with `XLSX.writeFile(wb, "bonusbridge_employee_template.xlsx")`.

### 3. Dependency
Add `xlsx-js-style` to `package.json`. Standard community `xlsx` ignores cell styles; `xlsx-js-style` is a drop-in fork that supports `font`, `fill`, etc., which is required for the grey italic example row.

### 4. Sidebar / navigation (if applicable)
If `AppSidebar.tsx` lists HR pages, add an "Employee Upload" link pointing to `/employee-upload`. If it's role-driven from a list, add the entry there.

## What I am NOT doing
- No Supabase schema changes, migrations, or new tables (per workspace rule).
- No upload parsing, validation, or insert logic — Step 2 is out of scope for this task.
- No changes to existing route guards or auth/entity contexts.
- No changes to `routeTree.gen.ts` (auto-generated by the TanStack Router Vite plugin).

## Verification checklist
- Sign in as `hr_rep` (or `ceo`), navigate to `/employee-upload`.
- "Step 1 — Download Template" section is visible at the top with the button.
- Click "Download Employee Template" → `bonusbridge_employee_template.xlsx` downloads.
- Open file: row 1 has the 9 headers in the exact order; row 2 shows the example values rendered in grey italic.
- Non-HR users hitting `/employee-upload` see "Access denied".
